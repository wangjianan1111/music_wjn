<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bjfu.notation_jh.dao.TmpNotationMapper">
    <resultMap id="BaseResultMap" type="com.bjfu.notation_jh.model.TmpNotation">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jan 15 11:05:12 CST 2019.
        -->
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="uid" property="uid" jdbcType="VARCHAR"/>
        <result column="img_url" property="imgUrl" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, uid, img_url
    </sql>

    <!--增加用户歌声合成记录-->
    <!--<insert id="addMusicSynthesisRecord" useGeneratedKeys="true" keyProperty="id" parameterType="com.bjfu.notation_jh.model.MusicSynthesis">-->
        <!--insert into t_music_synthesis (singer, music_name, music_type, isPublic,image_url,finished,uid,create_time)-->
        <!--values (#{musicSynthesis.singer}, #{musicSynthesis.musicName}, #{musicSynthesis.musicType},-->
                <!--#{musicSynthesis.isPublic}, #{musicSynthesis.imageUrl}, #{musicSynthesis.finished},#{musicSynthesis.uid},#{musicSynthesis.createTime})-->
    <!--</insert>-->

    <!-- 根据id查询前端参数表 -->
    <select id="queryById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        t_tmp_notation
        where id = #{id}
    </select>

    <!--保存临时表-->
    <insert id="saveTmpNotation" useGeneratedKeys="true" keyProperty="id" parameterType="com.bjfu.notation_jh.model.TmpNotation">
        insert into t_tmp_notation (uid,img_url)
        values (#{uid}, #{imgUrl})
    </insert>

    <!--更新临时表-->
    <update id="updateTmpNotation" parameterType="com.bjfu.notation_jh.model.TmpNotation">
        update t_tmp_notation
        <set>
            <if test="notationChange.content != null">
                img_url = #{tmpNotation.imgUrl,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{tmpNotation.id}
    </update>
</mapper>





















