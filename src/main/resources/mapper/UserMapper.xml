<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bjfu.notation_jh.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.bjfu.notation_jh.model.User">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jan 15 11:05:12 CST 2019.
        -->
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="login_id" property="loginId" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="modify_time" property="modifyTime" jdbcType="TIMESTAMP"/>
        <result column="nick_name" property="nickName" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="faceImage" property="faceImage" jdbcType="VARCHAR"/>
        <result column="birthday" property="birthday" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, phone, login_id, password, create_time, modify_time, nick_name,email,faceImage,birthday,sex
    </sql>

    <!--根据登录账号查询用户信息-->
    <select id="selectUserByLoginId" resultMap="BaseResultMap">
        select
          <include refid="Base_Column_List"/>
        from
          t_user
        where
        login_id = #{loginId}
    </select>

    <!--根据手机号查询用户信息-->
    <select id="selectUserByPhone" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        t_user
        where
        phone = #{phone}
    </select>

    <!--插入用户信息-->
    <insert id="insert" parameterType="com.bjfu.notation_jh.model.User">
        insert into t_user (id, phone, login_id,
                            password, create_time, modify_time,
                            nick_name)
        values (#{id}, #{phone}, #{loginId},
        #{password}, #{createTime}, #{modifyTime},#{nickName})
    </insert>

    <!--根据用户的手机号和登录密码查询用户信息-->
    <select id="selectUserByPhoneAndLoginPassword" resultMap="BaseResultMap">
        select
          <include refid="Base_Column_List"/>
        from
          t_user
        where
          login_password = #{0} and phone = #{1}
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.bjfu.notation_jh.model.User">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jan 15 11:05:12 CST 2019.
        -->
        update t_user
        <set>
            <if test="user.phone != null">
                phone = #{user.phone,jdbcType=VARCHAR},
            </if>
            <if test="user.loginId != null">
                login_id = #{user.loginId,jdbcType=VARCHAR},
            </if>
            <if test="user.password != null">
                password = #{user.password,jdbcType=VARCHAR},
            </if>
            <if test="user.createTime != null">
                create_time = #{user.createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="user.modifyTime != null">
                modify_time = #{user.modifyTime,jdbcType=TIMESTAMP},
            </if>
            <if test="user.nickName != null">
                nick_name = #{user.nickName,jdbcType=VARCHAR},
            </if>
            <if test="user.email != null">
                email = #{user.email,jdbcType=VARCHAR},
            </if>
            <if test="user.faceImage != null">
                faceImage = #{user.faceImage,jdbcType=VARCHAR},
            </if>
            <if test="user.birthday != null">
                birthday = #{user.birthday,jdbcType=VARCHAR},
            </if>
            <if test="user.sex != null">
                sex = #{user.sex,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{user.id}
    </update>

    <!--根据登录账号查询用户信息-->
    <select id="queryByLoginId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        t_user
        where
        login_id = #{loginId}
    </select>

    <!--根据id查询用户信息-->
    <select id="queryUserById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        t_user
        where
        id = #{id}
    </select>
</mapper>





















