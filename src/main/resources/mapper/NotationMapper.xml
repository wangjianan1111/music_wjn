<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bjfu.notation_jh.dao.NotaionMapper">
    <resultMap id="BaseResultMap" type="com.bjfu.notation_jh.model.BasicNotation">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jan 15 11:05:12 CST 2019.
        -->
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="uid" property="uid" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="modify_time" property="modifyTime" jdbcType="TIMESTAMP"/>
        <result column="notation_pic" property="notationPic" jdbcType="VARCHAR"/>
        <result column="notation_name" property="notationName" jdbcType="VARCHAR"/>
        <result column="notation_group" property="notationGroup" jdbcType="VARCHAR"/>
        <result column="notation_style" property="notationStyle" jdbcType="INTEGER"/>
        <result column="notation_tag" property="notationTag" jdbcType="VARCHAR"/>
        <result column="notation_desc" property="notationDesc" jdbcType="VARCHAR"/>
        <result column="notation_download_url" property="notationDownloadUrl" jdbcType="VARCHAR"/>
        <result column="isPublic" property="isPublic" jdbcType="INTEGER"/>
        <result column="weight" property="weight" jdbcType="INTEGER"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, uid, create_time, modify_time, notation_pic, notation_name, notation_group, notation_style, notation_tag, notation_desc, notation_download_url,isPublic,weight
    </sql>

    <!--根据条件查询简谱-->
    <select id="queryNotation" parameterType="com.bjfu.notation_jh.model.BasicNotation" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
         FROM t_basic_notation
        <where> isPublic = 0
            <if test="bn.id!= null and bn.id!=''">
                and id =#{bn.id}
            </if>
            <if test="bn.uid!= null and bn.uid!=''">
                and uid =#{bn.uid}
            </if>
            <if test="bn.notationName!= null and bn.notationName!=''">
                and notation_name like concat(concat('%',#{bn.notationName}),'%')
            </if>
            <if test="bn.notationGroup!= null and bn.notationGroup!=''">
                and notation_group=#{bn.notationGroup}
            </if>
            <if test="bn.notationStyle!= null and bn.notationStyle!='' or bn.notationStyle == 0">
                and notation_style=#{bn.notationStyle}
            </if>
            <if test="bn.notationTag!= null and bn.notationTag!=''">
                and notation_tag=#{bn.notationTag}
            </if>
        </where>
        ORDER BY id asc
        LIMIT #{pageNum},#{pageSize}
    </select>

    <!--根据条件查询简谱-->
    <select id="queryNotationCount" parameterType="com.bjfu.notation_jh.model.BasicNotation" resultType="java.lang.Integer">
        SELECT
        count(*)
        FROM t_basic_notation
        <where> isPublic = 0
            <if test="bn.uid!= null and bn.uid!=''">
                and uid =#{bn.uid}
            </if>
            <if test="bn.notationName!= null and bn.notationName!=''">
                and notation_name=#{bn.notationName}
            </if>
            <if test="bn.notationGroup!= null and bn.notationGroup!=''">
                and notation_group=#{bn.notationGroup}
            </if>
            <if test="bn.notationStyle!= null and bn.notationStyle!=''">
                and notation_style=#{bn.notationStyle}
            </if>
            <if test="bn.notationTag!= null and bn.notationTag!=''">
                and notation_tag=#{bn.notationTag}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <!--保存简谱-->
    <insert id="saveNotation" useGeneratedKeys="true" keyProperty="id" parameterType="com.bjfu.notation_jh.model.BasicNotation">
        insert into t_basic_notation (uid, create_time, notation_name, notation_style,  notation_download_url,isPublic)
        values (#{uid}, #{createTime}, #{notationName},
                #{notationStyle}, #{notationDownloadUrl}, #{isPublic})
    </insert>

    <!-- 删除简谱 -->
    <delete id="deleteNotaiton" parameterType="java.lang.Long">
        delete from t_basic_notation where
            id=#{id}
    </delete>
</mapper>





















